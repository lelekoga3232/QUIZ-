services:
  # Um serviço web para o aplicativo Flask
  - type: web
    name: quiz-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python render_start.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SOCKETIO_ASYNC_MODE
        value: threading
      - key: PYTHONUNBUFFERED
        value: 1
    plan: free
